// Этот файл генерируется утилитой md2html

class Article
{
	constructor(title, url)
	{
		this.title = title;
		this.url = url;
	}
}

// Хеш-таблица тег -> массив статей

let tagTable = {
	'нет' : [new Article('Тест', 'Тест.md.html'), ],
};
